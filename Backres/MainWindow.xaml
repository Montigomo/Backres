<Window x:Name="WindowMain1" x:Class="Backres.WindowMain"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Bacres"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="445.538">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition/>
			<RowDefinition Height="64"/>
		</Grid.RowDefinitions>
		<DataGrid x:Name="dataGridMain" HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch" AutoGenerateColumns="False" Grid.Row="0" CanUserReorderColumns="False" CanUserResizeRows="False" CanUserAddRows="False" BorderBrush="#FFAAAAAA" Tag="toggle" >
			<DataGrid.ContextMenu>
				<ContextMenu>
					<MenuItem x:Name="menuItemCmBackup" Header="Backup" Click="menuItemCmBackup_Click"/>
					<MenuItem x:Name="menuItemCmRestore" Header="Restore" Click="menuItemCmRestore_Click"/>
				</ContextMenu>
			</DataGrid.ContextMenu>
			<DataGrid.Columns>
				<DataGridTextColumn Header="Provider" Binding="{Binding Name}" MinWidth="150" />
				<DataGridTemplateColumn Header="Actions">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<StackPanel Orientation="Horizontal">
								<Button x:Name="bntBackup" Click="BntBackup_Click" Margin="3,0">Backup</Button>
								<Button x:Name="bntRestore" Margin="3,0" Click="BntRestore_Click" >Restore</Button>
							</StackPanel>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
			</DataGrid.Columns>
		</DataGrid>
		<Grid Grid.Row="1">
			<Grid.RowDefinitions>
				<RowDefinition/>
				<RowDefinition/>
			</Grid.RowDefinitions>
			<Button Grid.Row="0" x:Name="buttonTest" Content="TEST" Click="ButtonTest_Click" Height="21" Margin="10,6,379,5" />
			<TextBlock Grid.Row="1" TextWrapping="Wrap" Background="#FFE6E6E6" />
		</Grid>
	</Grid>
</Window>
